# Copy to .env and fill in, OR set these in your shell before running run.py.
# These are the SAME secrets already embedded in the workflow JSONs.

# Supabase project (service_role key — bypasses RLS, required for writes)
SUPABASE_URL=https://fuamnucdmfirddocfaig.supabase.co
SUPABASE_SERVICE_KEY=

# Google Generative AI key (gemini-embedding-001 for embeddings; gemini-2.5-flash for OCR)
GEMINI_API_KEY=
